# BH AA rollback impact expansion matrix

| Finding path | Root cause | Valid trigger? | Requires malformed input? | Single-node crash? | Restart loop? | Multi-node reproducible? | Local P2P propagation? | Network delay relevance | Severity if proven | Status |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|---|
| no-definition AA rollback crash direct `handleTrigger()` diagnostic | Response unit save mutates unit-props cache; secondary bounce rolls SQL back to `initial_balances`; final bounce calls `storage.readUnitProps()` with stale mem vs DB props | In-process only; source trigger is not replayable by fresh-process `handleAATriggers()` without malformed fixture state | No for diagnostic, but not a valid restart trigger | Yes, known signature `different props` | Killed: trigger is in-memory plus fake SQL output on existing MC unit | Killed for restart escalation | Not attempted; blocked until valid replay | None claimed | Medium/High locally; higher only if replay proven | In-process confirmed, restart unproven |
| valid AA trigger replay path | Searches canonical AA composer fixture units for a persisted unit row, authors, messages, payment output to the newly-created primary AA, and an `aa_triggers` replay through `aa_composer.handleAATriggers()` | No matching canonical trigger helper found yet; probe refuses to synthesize one | No | Killed unless a canonical fixture payment to the primary AA is found; test logs `BH_RESULT` and stale-cache markers | Killed for current fixture if `BH_RESULT.status=valid_trigger_path_killed` | Not proven | Not attempted | None claimed | Reduced to diagnostic direct-handleTrigger severity until a true canonical trigger path reaches `different props` | Added probe; no network impact claimed |
| AA definitions | AA response `definition` saved and `storage.insertAADefinitions()` called before later rollback; cache/message visibility can outlive SQL if cleanup incomplete | Yes | No | Possible via same stale cache class | Pending | Pending | Not attempted | None | High if stale activation enables value transfer | SQL rollback negative reported; cache visibility still worth matrix coverage |
| data feeds | Writer stores AA `data_feed` messages in `assocUnstableMessages`; AA formula data-feed lookup scans this cache for AA messages | Yes | No | Possible if rollback leaves stale cache | Pending | Pending | Not attempted | None | High if stale data feed drives payout | New lead; not yet tested in restart harness |
| asset issuance | Response saves asset definition/issue output before secondary bounce; rollback must clear asset rows, outputs, balances, caches | Yes | No | Possible | Not proven | Not proven | Not attempted | None | High if issued asset output survives | Negative result reported |
| payments | Primary response payment output saved before secondary bounce; rollback must remove outputs/inputs and balance deltas | Yes | No | Possible | Not proven | Not proven | Not attempted | None | Critical if attacker output survives | Negative result reported |
| response units | Writer stores SQL rows, memory cache entries, and kvstore `j\nunit` batch entries before later rollback | Yes | No | Yes if stale props read | Pending | Pending | Not attempted | None | High if stale unit can be replayed/served/used | Cache crash confirmed in-process; kvstore/sql survival not proven |
| nested secondary AAs | Recursive `handleTrigger()` with `bSecondary`; bounce of deeper child reverts or bubbles upward | Yes | No | Possible | Not proven | Not proven | Not attempted | None | High if partial nested output survives | Negative result reported |
| bounced responses | Primary bounce after rollback saves a new response unit; can read stale last-ball/unit props | Yes | No | Yes | Pending | Pending | Not attempted | None | Medium/High if restart replay | Known crash site area |
| repeated triggers | `aa_triggers` row is deleted only after `handleTrigger()` returns; crash before delete can leave row | Yes if fixture valid | No | Yes | Pending; requires row remains and replay re-crashes | Pending | Not attempted | None | High if deterministic restart loop | Must satisfy kill criteria |
| commit/rollback edges | SQL rollback to savepoint and memory rollback are manual and not symmetrical | Yes | No | Yes | Pending | Pending | Not attempted | None | High if stale state causes replay/delay | Root-cause family |
| validation/storage mismatch | `writer.saveJoint()` mutates storage caches after validation but before outer AA commit; `storage.readUnitProps()` later checks DB vs cache | Yes | No | Yes | Pending | Pending | Not attempted | None | Medium/High | Known signature |
| stable/unstable unit cache mismatch | `assocStableUnits` or `assocUnstableUnits` contains props diverging from SQL after rollback | Yes | No | Yes | Pending | Pending | Not attempted | None | Medium/High | Known signature references stale mem vs DB props |
| `assocUnstableMessages` | Writer adds data_feed/definition/system messages before later rollback; forget only per response unit | Yes | No | Possible | Pending | Pending | Not attempted | None | High for stale data-feed/definition activation | New high-signal lead |
| `assocStableUnits` | Stable unit props can be stale relative to DB after main-chain recalculation/rollback | Yes | No | Yes | Pending | Pending | Not attempted | None | Medium/High | Known mismatch names stable-like props |
| `assocUnstableUnits` | Reverted response units are removed by `forgetUnit()`, but derived links/parents may remain incomplete | Yes | No | Possible | Pending | Pending | Not attempted | None | Medium/High | Negative for outputs; cache differential remains root area |
| `aa_responses` | `addResponse()` inserts response rows before some later failures; SQL rollback should remove them | Yes | No | Possible | Not proven | Not proven | Not attempted | None | Medium; high only if dependent logic uses stale rows | Negative for simple rollback not exhaustive |
| `aa_triggers` | Pending triggers survive crash before delete; startup processes leftovers | Yes if fixture valid | No | Yes | Pending | Pending | Not attempted | None | High for restart loop/delayed processing | Needs valid fresh fixture |
| `writer.js` saved-unit side effects | Writer mutates SQL, cache, `assocUnstableMessages`, AA definitions, and kvstore batch inside outer AA tx | Yes | No | Yes | Pending | Pending | Not attempted | None | High if cleanup incomplete | Core root-cause mechanism |
| `main_chain.js` MC recalculation side effects | Saving response units can call main-chain update/cache mutation in writer; rollback may not reset all memory state | Yes | No | Yes | Pending | Pending | Not attempted | None | Medium/High if props diverge | Candidate contributor to known mismatch |
